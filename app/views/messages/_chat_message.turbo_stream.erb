<%= turbo_stream.action(:append, "chat") do %>
  <div class="<%= ["message", dom_id(user)].join(" ") %>">
    <h4 class="username">
      <%= user.username.truncate(14) %>
    </h4>
    <p>
      <%= text %>
    </p>
  </div>
<% end %>