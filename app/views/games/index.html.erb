<p style="color: green"><%= notice %></p>

<h1>Games</h1>

<%= turbo_stream_from "lobby", channel: LobbyChannel %>

<div id="games" data-controller="<%= "lobby" if true %>">
  <%= render partial: "entry", collection: @games %>
</div>

<%= link_to "New game", new_game_path %>
