<%= form_with(model: joke, url: [@game, @round, joke]) do |form| %>
  <% if joke.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(joke.errors.count, "error") %> prohibited this joke from being saved:</h2>

      <ul>
        <% joke.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :punchline, style: "display: block" %>
    <%= form.text_field :punchline %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
