<%= form_with(model: award, url: award_path) do |form| %>
  <% if award.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(award.errors.count, "error") %> prohibited this award from being saved:</h2>

      <ul>
        <% award.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :user_id %>
  </div>

  <div>
    <%= form.label :present_id, style: "display: block" %>
    <%= form.select :present_id, Present.all.pluck(:name, :id) %>
  </div>

  <div>
    <%= form.label :signature, style: "display: block" %>
    <%= form.text_field :signature %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
